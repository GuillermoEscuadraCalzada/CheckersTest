using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Checkers
{
    public class Minimax : MonoBehaviour
    {

        
        //public Chip MiniMax(Chip state, int depth, bool isMaximizingPlayer, int alpha, int beta)
        //{
        //    if (depth == 0)
        //        return;

        //    int currentValue;
        //    Chip childChip;
        //    Chip nextChip;

        //    if (isMaximizingPlayer)
        //    {
        //        currentValue = Int32.MinValue;
        //        nextChip = null;
        //        List<Chip> chips = state.CheckForPlayerPosibleMoves(chips, ref globalAviableMoves);


        //        foreach(Chip currentChip in chips)
        //        {
        //            childChip = MiniMax(currentChip, depth - 1, false, alpha, beta);

        //        }
        //    }
        //}

    }

}
